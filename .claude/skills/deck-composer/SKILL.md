---
name: deck-composer
description: Use when the user wants to turn source content (docs, notes, URLs, briefs) into a finished slide deck in this repo. Superseded by experience-composer, which covers slide decks and four other surfaces — this is a pointer stub. Triggers on "create/make/build a slide deck / presentation / slide pack from …".
---

# Deck Composer → see Experience Composer

This skill has been generalized into **`experience-composer`**, which composes a finished, rendered experience on any of five surfaces (slide deck, dashboard, project page, personal page, technical explainer). The proven slide-deck flow lives there verbatim as the slide-deck path — same phases 0–6, same `compose_slide_deck` + `validate_fill` tools, same `references/fill-authoring.md` and `references/scaffold-and-verify.md`. For a slide deck, use `experience-composer` and let its Phase 2 pick `compose_slide_deck`.
