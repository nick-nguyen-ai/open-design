{
  "note": "Two compose_slide_deck calls this run. Call 1 pinned deck-technical-architecture-explanation per the brief and errored UNKNOWN_TEMPLATE (that world is not templatized — no worldtemplate manifest, not in world-templates.json). Call 2 pinned deck-cloud-migration (cutover), the technical-blueprint grammar's live slide-deck template; its full result is under `result` verbatim.",
  "pinAttempt": {
    "pinTemplateId": "deck-technical-architecture-explanation",
    "error": {
      "code": "UNKNOWN_TEMPLATE",
      "message": "pinTemplateId \"deck-technical-architecture-explanation\" is not a live slide-deck template.",
      "details": ["Live slide-deck templates: cutover (deck-cloud-migration), dgm-blueprint (deck-dgm-blueprint), dgm-circuit (deck-dgm-circuit), dgm-gazette (deck-dgm-gazette), dgm-isometric (deck-dgm-isometric), dgm-sketchnote (deck-dgm-sketchnote), quarter (deck-quarterly-business-review), tminus (deck-product-launch)."],
      "remediation": ["Pin one of this surface's template ids/experienceIds (typically from a previous call's alternatives), or drop pinTemplateId to let scoring decide."],
      "requestId": "ae1cd5ca-669b-4211-87e7-03ed07d36113"
    }
  },
  "result": {
    "worldTemplateId": "cutover",
    "experienceId": "deck-cloud-migration",
    "rationale": "Pinned 'cutover' (deck-cloud-migration, art-directed style) by explicit pinTemplateId — scoring bypassed; score shown for transparency.",
    "evidence": ["cutover (deck-cloud-migration): audienceOverlap 0×2=0 + intentMatch 0 + corporateFit 1 = 1"],
    "alternatives": [
      {
        "worldTemplateId": "cutover",
        "experienceId": "deck-cloud-migration",
        "score": 1,
        "scoreBreakdown": "cutover (deck-cloud-migration): audienceOverlap 0×2=0 + intentMatch 0 + corporateFit 1 = 1",
        "style": "art-directed",
        "mood": "light",
        "grammarId": "technical-blueprint",
        "guidance": [
          "The draw.io working-file idiom: a flat dot-grid canvas, exact orthogonal connectors with port dots, pastel-filled system boxes with type badges, a layers legend chip in the chrome, and selection handles on each slide’s focus node.",
          "Exactly one estate node carries the \"stays\" anomaly disposition: padlocked, heavier-stroked, in the SAME place on both the current and target estate; its badge is the verbatim anomaly text, echoed on both estate slides.",
          "The synthetic-estate notice is required and prints on every slide’s print foot and the footer.",
          "Both estates expose a hidden zone-grouped accessible mirror derived from the same node data — the current estate reads all on-prem, the target splits into a cloud landing zone plus the on-prem zone that keeps the fixed point.",
          "Node and edge counts are bounded so the shared estate canvas, the swimlanes, and the flow diagram stay composed for any schema-valid fill.",
          "Motion level 2 (data-ink draw); the mood is locked light."
        ]
      }
    ],
    "fillSkeleton": {
      "sections": [
        {
          "kind": "title",
          "purpose": "The file-header cover — draw.io file/rev, programme, editors, the two-line thesis, and the notice.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "deck.code", "type": "text", "required": true, "limits": { "maxChars": 24 }, "guidance": "The short programme code shown first on every kicker row, e.g. \"CBM-CUTOVER\"." }, "guidance": "The short programme code shown first on every kicker row, e.g. \"CBM-CUTOVER\".", "example": "CBM-CUTOVER" },
            { "spec": { "name": "deck.world", "type": "text", "required": true, "limits": { "maxChars": 24 }, "guidance": "The world name paired with the code on the kicker and the browser title, e.g. \"The Cutover\"." }, "guidance": "The world name paired with the code on the kicker and the browser title, e.g. \"The Cutover\".", "example": "The Cutover" },
            { "spec": { "name": "deck.file", "type": "text", "required": true, "limits": { "maxChars": 32 }, "guidance": "The draw.io working-file name, e.g. \"cutover-plan.drawio\" — shown in the file tab and every kicker row." }, "guidance": "The draw.io working-file name, e.g. \"cutover-plan.drawio\" — shown in the file tab and every kicker row.", "example": "cutover-plan.drawio" },
            { "spec": { "name": "deck.rev", "type": "text", "required": true, "limits": { "maxChars": 16 }, "guidance": "The revision marker, e.g. \"rev 14\" — paired with the file name and echoed on sign-off." }, "guidance": "The revision marker, e.g. \"rev 14\" — paired with the file name and echoed on sign-off.", "example": "rev 14" },
            { "spec": { "name": "deck.programme", "type": "text", "required": true, "limits": { "maxChars": 44 }, "guidance": "Programme line above the thesis, e.g. \"CORE BANKING · CLOUD MIGRATION\"." }, "guidance": "Programme line above the thesis, e.g. \"CORE BANKING · CLOUD MIGRATION\".", "example": "CORE BANKING · CLOUD MIGRATION" },
            { "spec": { "name": "deck.editors", "type": "text", "required": true, "limits": { "maxChars": 44 }, "guidance": "The draw.io editors line, e.g. \"A. VOSS · L. CHEN · PLATFORM\"." }, "guidance": "The draw.io editors line, e.g. \"A. VOSS · L. CHEN · PLATFORM\".", "example": "A. VOSS · L. CHEN · PLATFORM" },
            { "spec": { "name": "deck.notice", "type": "text", "required": true, "limits": { "maxChars": 52 }, "guidance": "REQUIRED synthetic-estate notice printed on every slide foot and the footer." }, "guidance": "REQUIRED synthetic-estate notice printed on every slide foot and the footer.", "example": "<text, ≤52 chars>" },
            { "spec": { "name": "thesis.line1", "type": "text", "required": true, "limits": { "maxChars": 32 }, "guidance": "First display line of the cover thesis, e.g. \"Move everything.\"." }, "guidance": "First display line of the cover thesis, e.g. \"Move everything.\".", "example": "Move everything." },
            { "spec": { "name": "thesis.line2", "type": "text", "required": true, "limits": { "maxChars": 32 }, "guidance": "Second display line — the fixed-point twist, e.g. \"Except one box.\"." }, "guidance": "Second display line — the fixed-point twist, e.g. \"Except one box.\".", "example": "Except one box." },
            { "spec": { "name": "thesis.standfirst", "type": "longtext", "required": true, "limits": { "maxChars": 260 }, "guidance": "One paragraph framing the migration around its single fixed point." }, "guidance": "One paragraph framing the migration around its single fixed point.", "example": "<longtext, ≤260 chars>" }
          ]
        },
        {
          "kind": "current",
          "purpose": "The current estate diagram on the shared draw.io canvas; selection handles on the focus node.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "nodes", "type": "nodes", "required": true, "limits": { "minItems": 5, "maxItems": 9 }, "guidance": "The estate systems (id, label, kind app|data|integration, target zone, disposition). Geometry (current cx/cy + target tx/ty) is OPTIONAL and bounded to the canvas when given; omit it and the template auto-lays the node by zone (on-prem left, cloud right). Exactly one carries disposition \"stays\" — the padlocked anomaly, in the same place on both estates." }, "guidance": "The estate systems (id, label, kind app|data|integration, target zone, disposition). Geometry (current cx/cy + target tx/ty) is OPTIONAL and bounded to the canvas when given; omit it and the template auto-lays the node by zone (on-prem left, cloud right). Exactly one carries disposition \"stays\" — the padlocked anomaly, in the same place on both estates.", "example": "<5–9 nodes>" },
            { "spec": { "name": "currentEdges", "type": "edges", "required": true, "limits": { "minItems": 4, "maxItems": 10 }, "guidance": "Current-estate orthogonal connectors: from/to node ids, OPTIONAL exit/enter ports (l|r|t|b) derived from node positions when omitted, and an optional wire label (e.g. \"sql · 4ms\")." }, "guidance": "Current-estate orthogonal connectors: from/to node ids, OPTIONAL exit/enter ports (l|r|t|b) derived from node positions when omitted, and an optional wire label (e.g. \"sql · 4ms\").", "example": "sql · 4ms" },
            { "spec": { "name": "currentFocus", "type": "text", "required": true, "limits": { "maxChars": 40 }, "guidance": "Node id given draw.io selection handles on the current estate slide — usually the fixed point." }, "guidance": "Node id given draw.io selection handles on the current estate slide — usually the fixed point.", "example": "<text, ≤40 chars>" },
            { "spec": { "name": "estateNotes.current", "type": "longtext", "required": true, "limits": { "maxChars": 180 }, "guidance": "One-line caption under the current-estate diagram — what the estate is today and which node is the selected fixed point." }, "guidance": "One-line caption under the current-estate diagram — what the estate is today and which node is the selected fixed point.", "example": "<longtext, ≤180 chars>" }
          ]
        },
        {
          "kind": "target",
          "purpose": "The target estate diagram — same canvas, moved; the on-prem zone boxes the node that stays.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "targetEdges", "type": "edges", "required": true, "limits": { "minItems": 4, "maxItems": 10 }, "guidance": "Target-estate orthogonal connectors after the move; same shape as currentEdges (ports optional, derived when omitted)." }, "guidance": "Target-estate orthogonal connectors after the move; same shape as currentEdges (ports optional, derived when omitted).", "example": "<4–10 edges>" },
            { "spec": { "name": "targetFocus", "type": "text", "required": true, "limits": { "maxChars": 40 }, "guidance": "Node id given selection handles on the target estate slide — usually the system being refactored." }, "guidance": "Node id given selection handles on the target estate slide — usually the system being refactored.", "example": "<text, ≤40 chars>" },
            { "spec": { "name": "estateNotes.target", "type": "longtext", "required": true, "limits": { "maxChars": 180 }, "guidance": "One-line caption under the target-estate diagram — what changes after the move; the fixed point stays boxed in its on-prem zone." }, "guidance": "One-line caption under the target-estate diagram — what changes after the move; the fixed point stays boxed in its on-prem zone.", "example": "<longtext, ≤180 chars>" }
          ]
        },
        {
          "kind": "delta",
          "purpose": "Three columns — what moves, what dies, what stays.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "headlines.delta", "type": "text", "required": true, "limits": { "maxChars": 64 }, "guidance": "Editorial headline for the moves/dies/stays columns, e.g. \"Three columns say the whole plan.\"." }, "guidance": "Editorial headline for the moves/dies/stays columns, e.g. \"Three columns say the whole plan.\".", "example": "Three columns say the whole plan." },
            { "spec": { "name": "delta.moves", "type": "items", "required": true, "limits": { "minItems": 1, "maxItems": 6 }, "guidance": "Systems that migrate; each a system name and a one-line disposition note." }, "guidance": "Systems that migrate; each a system name and a one-line disposition note.", "example": "<1–6 items>" },
            { "spec": { "name": "delta.dies", "type": "items", "required": true, "limits": { "minItems": 1, "maxItems": 4 }, "guidance": "Systems retired or replaced; each a system name and a one-line note." }, "guidance": "Systems retired or replaced; each a system name and a one-line note.", "example": "<1–4 items>" },
            { "spec": { "name": "delta.stays", "type": "items", "required": true, "limits": { "minItems": 1, "maxItems": 3 }, "guidance": "Systems that stay put; the fixed point belongs here." }, "guidance": "Systems that stay put; the fixed point belongs here.", "example": "<1–3 items>" }
          ]
        },
        {
          "kind": "waves",
          "purpose": "The migration waves as a swimlane — each a weekend, its systems as chips.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "headlines.waves", "type": "text", "required": true, "limits": { "maxChars": 64 }, "guidance": "Editorial headline for the migration waves; must match the wave count you supply, e.g. \"Three weekends, three waves.\"." }, "guidance": "Editorial headline for the migration waves; must match the wave count you supply, e.g. \"Three weekends, three waves.\".", "example": "Three weekends, three waves." },
            { "spec": { "name": "waves", "type": "items", "required": true, "limits": { "minItems": 2, "maxItems": 5 }, "guidance": "Each wave has a name, a when, one-to-four system chips (label + kind), and a one-line note." }, "guidance": "Each wave has a name, a when, one-to-four system chips (label + kind), and a one-line note.", "example": "<2–5 items>" }
          ]
        },
        {
          "kind": "cutover",
          "purpose": "The cutover-night sequence as a comp.flow-diagram, one path down to a validation gate.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "headlines.cutover", "type": "text", "required": true, "limits": { "maxChars": 64 }, "guidance": "Editorial headline for the cutover-night sequence, e.g. \"Cutover night, one path down.\"." }, "guidance": "Editorial headline for the cutover-night sequence, e.g. \"Cutover night, one path down.\".", "example": "Cutover night, one path down." },
            { "spec": { "name": "cutoverFlow.nodes", "type": "nodes", "required": true, "limits": { "minItems": 3, "maxItems": 9 }, "guidance": "Flow steps (id, label, kind start|process|decision|end|data); include one decision gate that branches to open vs. rollback." }, "guidance": "Flow steps (id, label, kind start|process|decision|end|data); include one decision gate that branches to open vs. rollback.", "example": "<3–9 nodes>" },
            { "spec": { "name": "cutoverFlow.edges", "type": "edges", "required": true, "limits": { "minItems": 2, "maxItems": 10 }, "guidance": "Flow transitions (from/to node ids); label the decision branches (e.g. \"pass\" / \"fail\")." }, "guidance": "Flow transitions (from/to node ids); label the decision branches (e.g. \"pass\" / \"fail\").", "example": "pass" }
          ]
        },
        {
          "kind": "sync",
          "purpose": "The data sync & validation plan as a numbered list.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "headlines.sync", "type": "text", "required": true, "limits": { "maxChars": 64 }, "guidance": "Editorial headline for the data sync plan, e.g. \"Nothing cuts over until the data agrees.\"." }, "guidance": "Editorial headline for the data sync plan, e.g. \"Nothing cuts over until the data agrees.\".", "example": "Nothing cuts over until the data agrees." },
            { "spec": { "name": "syncPlan", "type": "items", "required": true, "limits": { "minItems": 3, "maxItems": 7 }, "guidance": "Ordered sync stages; each a stage name and a one-line detail — nothing cuts over until the data agrees." }, "guidance": "Ordered sync stages; each a stage name and a one-line detail — nothing cuts over until the data agrees.", "example": "<3–7 items>" }
          ]
        },
        {
          "kind": "rollback",
          "purpose": "The bespoke rollback tree from the validation gate.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "headlines.rollback", "type": "text", "required": true, "limits": { "maxChars": 64 }, "guidance": "Editorial headline for the rollback tree, e.g. \"If it fails, we’re back by morning.\"." }, "guidance": "Editorial headline for the rollback tree, e.g. \"If it fails, we’re back by morning.\".", "example": "If it fails, we’re back by morning." },
            { "spec": { "name": "rollback.nodes", "type": "nodes", "required": true, "limits": { "minItems": 3, "maxItems": 7 }, "guidance": "Rollback-tree nodes (id, label, tone root|ok|abort; optional x/y auto-laid by depth when omitted); the root is the validation gate." }, "guidance": "Rollback-tree nodes (id, label, tone root|ok|abort; optional x/y auto-laid by depth when omitted); the root is the validation gate.", "example": "<3–7 nodes>" },
            { "spec": { "name": "rollback.edges", "type": "edges", "required": true, "limits": { "minItems": 2, "maxItems": 8 }, "guidance": "Rollback-tree edges (from/to node ids) — the pass branch and the fail chain." }, "guidance": "Rollback-tree edges (from/to node ids) — the pass branch and the fail chain.", "example": "<2–8 edges>" },
            { "spec": { "name": "rollback.note", "type": "longtext", "required": true, "limits": { "maxChars": 240 }, "guidance": "One paragraph on why rollback is bounded to the maintenance window." }, "guidance": "One paragraph on why rollback is bounded to the maintenance window.", "example": "<longtext, ≤240 chars>" }
          ]
        },
        {
          "kind": "risk",
          "purpose": "The cutover risk register as a comp.status-list.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "headlines.risk", "type": "text", "required": true, "limits": { "maxChars": 64 }, "guidance": "Editorial headline for the risk register; must be true of the risks you supply, e.g. \"The risk register, one open item.\"." }, "guidance": "Editorial headline for the risk register; must be true of the risks you supply, e.g. \"The risk register, one open item.\".", "example": "The risk register, one open item." },
            { "spec": { "name": "risks", "type": "items", "required": true, "limits": { "minItems": 2, "maxItems": 6 }, "guidance": "Each risk: a label, a status (success|warning|danger|info|neutral), and a description." }, "guidance": "Each risk: a label, a status (success|warning|danger|info|neutral), and a description.", "example": "<2–6 items>" }
          ]
        },
        {
          "kind": "closing",
          "purpose": "The rev sign-off — the decisions this review must grant.",
          "repeats": { "min": 1, "max": 1 },
          "slots": [
            { "spec": { "name": "signoff.title", "type": "text", "required": true, "limits": { "maxChars": 40 }, "guidance": "The sign-off headline, e.g. \"Ready to sign rev 14.\"." }, "guidance": "The sign-off headline, e.g. \"Ready to sign rev 14.\".", "example": "Ready to sign rev 14." },
            { "spec": { "name": "signoff.detail", "type": "longtext", "required": true, "limits": { "maxChars": 240 }, "guidance": "One paragraph stating what the review must decide." }, "guidance": "One paragraph stating what the review must decide.", "example": "<longtext, ≤240 chars>" },
            { "spec": { "name": "signoff.approvals", "type": "items", "required": true, "limits": { "minItems": 1, "maxItems": 5 }, "guidance": "Each approval: a role and the decision it must make, with a sign-off box." }, "guidance": "Each approval: a role and the decision it must make, with a sign-off box.", "example": "<1–5 items>" }
          ]
        }
      ],
      "craftGuarantees": [
        "The draw.io working-file idiom: a flat dot-grid canvas, exact orthogonal connectors with port dots, pastel-filled system boxes with type badges, a layers legend chip in the chrome, and selection handles on each slide’s focus node.",
        "Exactly one estate node carries the \"stays\" anomaly disposition: padlocked, heavier-stroked, in the SAME place on both the current and target estate; its badge is the verbatim anomaly text, echoed on both estate slides.",
        "The synthetic-estate notice is required and prints on every slide’s print foot and the footer.",
        "Both estates expose a hidden zone-grouped accessible mirror derived from the same node data — the current estate reads all on-prem, the target splits into a cloud landing zone plus the on-prem zone that keeps the fixed point.",
        "Node and edge counts are bounded so the shared estate canvas, the swimlanes, and the flow diagram stay composed for any schema-valid fill.",
        "Motion level 2 (data-ink draw); the mood is locked light."
      ]
    }
  }
}
